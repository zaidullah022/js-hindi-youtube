const score = 500

const balance = new Number(100)
console.log(balance);

console.log(balance.toString().length); //toString() converts the number to string and then we can get the length of the string

console.log(balance.toFixed(2)); //toFixed() method formats a number using fixed-point notation and returns a string representation of the number with a specified number of digits after the decimal point.

console.log(balance.toString().length);
